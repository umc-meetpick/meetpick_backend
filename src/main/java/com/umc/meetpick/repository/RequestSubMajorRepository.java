package com.umc.meetpick.repository;

import com.umc.meetpick.entity.mapping.RequestSubMajor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestSubMajorRepository extends JpaRepository<RequestSubMajor, Long> {
}
